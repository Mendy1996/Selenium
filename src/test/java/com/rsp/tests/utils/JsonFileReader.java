/*
package com.rsp.tests.utils;

import com.jayway.jsonpath.JsonPath;

public class JsonFileReader {

    public static String getTestData(String json,String testName,Object key) {
            String path = "$." + testName + "." + key;
            System.out.println(path);
            return  JsonPath.read(json, "$." + testName + "." + key).toString();
    }
}
*/
